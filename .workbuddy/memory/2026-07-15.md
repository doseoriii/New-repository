# 2026-07-15

## 海外项目跟进指南 Web 应用
- 需求：多用户、各自密码登录；普通成员只能看/编辑自己登记的客户与项目；总管理员看全部、可增删成员/改密码。
- 实现：零外部依赖 Node.js（内置 http/crypto/fs），数据存 `data.json`，密码用 scrypt 加密，token 会话存内存。
- 模块：客户追踪、项目跟进、管理员面板（概览统计+最近登录记录）。
- 默认管理员 admin/admin123（首次启动 seed，已提醒修改）。
- 启动：`node server.js`，访问 http://localhost:3000
- 测试通过：数据隔离、越权拦截(403)、管理员可见全部、静态资源服务均正常。
- 注意：token 存内存，重启服务后所有用户需重新登录；data.json 需定期备份。

## 云端部署准备（19:26 后）
- 用户希望别人在各自电脑登录 → 选了「部署到 Node 托管平台」。
- CloudStudio 静态部署不支持后端，已说明并改走 Railway/Render。
- 改造：server.js 支持 `DATA_DIR` 环境变量（便于挂持久卷）；package.json 加 engines node>=18。
- 新增部署配置：railway.json、render.yaml、.gitignore；README 增加部署与持久化提醒。
- 已 `git init` 并提交（commit 1aca3ec），data.json 被 gitignore。
- 重要提醒：免费云平台文件系统临时，必须挂持久卷 + 设 DATA_DIR=/data，否则重启丢数据。
- 我无法从本环境直接点部署（需用户连 GitHub/Railway 账号），已准备好代码与步骤由用户完成上线。

## 改用 MongoDB Atlas 云端数据库（19:49 后）
- 用户部署到 Railway 成功（域名 overseas-project-tracker-production-260e.up.railway.app，HTTP 200 可访问），但 Railway 免费版 Settings 无 Volumes 选项，无法挂持久卷 → data.json 方案会丢数据。
- 决策：方案 C —— 改用免费 MongoDB Atlas 云端数据库，数据不再依赖 Railway 磁盘。
- server.js 已重写：移除 fs/data.json，改用 mongodb 驱动（v6.3.0）；连接串从环境变量 `MONGO_URI` 读取；启动时若无可 admin 则 seed；users/clients/projects/logins 改为集合操作（全部 async）。接口签名未变，前端无需改。
- package.json 加 `dependencies.mongodb`；已 `npm install` 本地装好（node_modules 被 gitignore）；server.js 语法校验通过。
- commit 3931599。本地未端到端测试（缺 Atlas 连接串）。
- 待办：用户注册 MongoDB Atlas → 建 M0 免费集群 → 建 DB 用户+网络允许 0.0.0.0/0 → 复制连接串 → 在 Railway Variables 设 `MONGO_URI` 并 git push 触发重部署。Railway 里原 DATA_DIR 变量可删（已无用）。

## 完成：MongoDB Atlas + Railway 上线（20:24 左右）
- 用户完成：Atlas M0 免费集群（Hong Kong）、DB 用户 `txyonlyloveyou_db_user`、网络访问 0.0.0.0/0、Railway `MONGO_URI` 变量、`DATA_DIR` 删除。
- 问题：首次部署（commit 3931599）时 `MONGO_URI` 尚未设置，导致容器反复重启、健康检查失败。
- 解决：设好 `MONGO_URI` 后重新 Deploy，公网地址 https://overseas-project-tracker-production-260e.up.railway.app 已正常响应（HTTP 200，响应 <0.5s）。
- 验证：admin/admin123 登录成功，返回 token 与 admin 角色；说明 MongoDB 连接正常、初始管理员已 seed。
- 后续：用户需在浏览器中打开网址，先改 admin 默认密码，再给同事创建账号。
- 持久化：数据现在存于 MongoDB Atlas，Railway 重启/重部署不会丢失。

## 完成：MongoDB Atlas + Railway 正式上线（21:30 后）
- 部署 commit b1d5658 已 ACTIVE，服务显示 Online。
- 中文验证通过：用 Python 发送 UTF-8 请求，创建「测试客户-Python」写入/读取均正常，无乱码。说明 server.js 的 `req.setEncoding('utf8')` 修复已在线上生效。
- 之前用 curl 在命令行测试出现乱码，是因为本机 shell 编码环境不是 UTF-8，非服务端问题。
- 已清理掉测试阶段产生的乱码客户/项目数据，数据库恢复干净（users: 1, clients: 0, projects: 0）。
- 公网地址：https://overseas-project-tracker-production-260e.up.railway.app
- 默认管理员：admin / admin123（用户上线后需先修改）。
- 后续：用户用浏览器打开网址，先改 admin 密码，再创建同事账号；数据永久保存在 MongoDB Atlas，Railway 重启不会丢失。

## 客户字段改造：标签 → 最近联系日期，并展示关联项目（22:00 后）
- 用户反馈：客户表单不需要「标签」，改成「最近联系日期」；在客户详情/编辑弹窗下方展示该客户的「目前项目」。
- 改造：
  - server.js：`sanitizeClient` 字段列表去掉 `tags`，新增 `lastContactDate`；API 返回/保存均兼容（旧 `tags` 数据不显示，但不会被删除）。
  - public/app.js：`renderClients()` 表头增加「最近联系」「项目数」；`openClientModal()` 去掉标签输入，改为日期输入；弹窗下方按 `clientId` 匹配 `state.projects` 渲染关联项目列表。
  - public/styles.css：补充关联项目列表的样式，保持移动端适配。
- 推送到 GitHub（commit 449f011），Railway 显示部署成功，但用户浏览器/我的 curl 都拿到旧 `app.js`（仍含 `f-tags`）。
- 诊断：GitHub 远程 `main` 已是最新 commit（`git ls-remote` 确认），但 Railway 实际服务旧代码；root cause 是静态文件未设 `Cache-Control`，Railway 把旧 `app.js` 缓存了。
- 解决：
  - server.js 的 `serveStatic()` 增加 `Cache-Control: no-cache` 响应头。
  - 再次推送新 commit（22ec04d）触发 Railway 重新部署。
- 22:15 左右用户确认页面已更新：客户表单出现「最近联系日期」，列表出现「项目数」列；功能生效。
- 我侧 curl 仍偶尔命中旧缓存节点，说明 Railway 边缘缓存/节点同步有短暂延迟，但 no-cache 头已加，后续会全局一致。
- 公网地址仍为：https://overseas-project-tracker-production-260e.up.railway.app

## 登录界面移除默认管理员提示（22:18 后）
- 用户要求：登录界面不要显示默认管理员账号密码提示（admin/admin123），他自己记着即可。
- 处理：删除 `public/index.html` 第 25 行 `<p class="login-tip">默认管理员：admin / admin123（请登录后修改）</p>`。
- 账号本身保留（数据库 seed 的 admin/admin123 不变），仅移除界面提示文字。
- 推送 commit 9644536 触发 Railway 重新部署。
- 经验：Railway 自动部署后偶发边缘缓存/节点同步延迟，若线上未更新需手动 Redeploy 或浏览器 Ctrl+Shift+R 强刷。

## 移动端适配（22:20 后）
- 用户希望手机上也能正常使用（界面适配手机）。
- 原样式仅有极弱 @media（表单单列），表格在窄屏会挤压、弹窗贴边、iOS 输入框聚焦缩放。
- 方案：窄屏（≤720px）响应式卡片——表格每行变卡片（CSS `display:block` + `td::before { content: attr(data-label) }` 显示列名）；桌面端完全不变。
- 改动：
  - public/app.js：给客户表、项目表、管理员面板三张表（概览/成员/登录记录）的所有 `<td>` 加 `data-label="列名"`；node --check 通过。
  - public/styles.css：重写 @media(≤720px)：内容/顶栏收窄内边距；标签栏横向滚动；工具栏按钮满行；弹窗全屏（border-radius:0, height:100vh）；输入框 16px 防 iOS 缩放；表格转卡片（隐藏 thead、tr 加卡片样式、td 右对齐+左列名）；操作按钮加大触控区。
  - index.html 已有 viewport meta，无需改。
- 推送 commit 2146926 触发 Railway 重新部署。
- 兼容性说明：纯 CSS 卡片方案，不依赖 JS 设备检测；后续新增列需记得在 td 加 data-label 才能在手机端显示列名。
