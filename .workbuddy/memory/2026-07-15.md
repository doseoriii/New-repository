# 2026-07-15

## 海外项目跟进指南 Web 应用
- 需求：多用户、各自密码登录；普通成员只能看/编辑自己登记的客户与项目；总管理员看全部、可增删成员/改密码。
- 实现：零外部依赖 Node.js（内置 http/crypto/fs），数据存 `data.json`，密码用 scrypt 加密，token 会话存内存。
- 模块：客户追踪、项目跟进、管理员面板（概览统计+最近登录记录）。
- 默认管理员 admin/admin123（首次启动 seed，已提醒修改）。
- 启动：`node server.js`，访问 http://localhost:3000
- 测试通过：数据隔离、越权拦截(403)、管理员可见全部、静态资源服务均正常。
- 注意：token 存内存，重启服务后所有用户需重新登录；data.json 需定期备份。

## 云端部署准备（19:26 后）
- 用户希望别人在各自电脑登录 → 选了「部署到 Node 托管平台」。
- CloudStudio 静态部署不支持后端，已说明并改走 Railway/Render。
- 改造：server.js 支持 `DATA_DIR` 环境变量（便于挂持久卷）；package.json 加 engines node>=18。
- 新增部署配置：railway.json、render.yaml、.gitignore；README 增加部署与持久化提醒。
- 已 `git init` 并提交（commit 1aca3ec），data.json 被 gitignore。
- 重要提醒：免费云平台文件系统临时，必须挂持久卷 + 设 DATA_DIR=/data，否则重启丢数据。
- 我无法从本环境直接点部署（需用户连 GitHub/Railway 账号），已准备好代码与步骤由用户完成上线。

## 改用 MongoDB Atlas 云端数据库（19:49 后）
- 用户部署到 Railway 成功（域名 overseas-project-tracker-production-260e.up.railway.app，HTTP 200 可访问），但 Railway 免费版 Settings 无 Volumes 选项，无法挂持久卷 → data.json 方案会丢数据。
- 决策：方案 C —— 改用免费 MongoDB Atlas 云端数据库，数据不再依赖 Railway 磁盘。
- server.js 已重写：移除 fs/data.json，改用 mongodb 驱动（v6.3.0）；连接串从环境变量 `MONGO_URI` 读取；启动时若无可 admin 则 seed；users/clients/projects/logins 改为集合操作（全部 async）。接口签名未变，前端无需改。
- package.json 加 `dependencies.mongodb`；已 `npm install` 本地装好（node_modules 被 gitignore）；server.js 语法校验通过。
- commit 3931599。本地未端到端测试（缺 Atlas 连接串）。
- 待办：用户注册 MongoDB Atlas → 建 M0 免费集群 → 建 DB 用户+网络允许 0.0.0.0/0 → 复制连接串 → 在 Railway Variables 设 `MONGO_URI` 并 git push 触发重部署。Railway 里原 DATA_DIR 变量可删（已无用）。

## 完成：MongoDB Atlas + Railway 上线（20:24 左右）
- 用户完成：Atlas M0 免费集群（Hong Kong）、DB 用户 `txyonlyloveyou_db_user`、网络访问 0.0.0.0/0、Railway `MONGO_URI` 变量、`DATA_DIR` 删除。
- 问题：首次部署（commit 3931599）时 `MONGO_URI` 尚未设置，导致容器反复重启、健康检查失败。
- 解决：设好 `MONGO_URI` 后重新 Deploy，公网地址 https://overseas-project-tracker-production-260e.up.railway.app 已正常响应（HTTP 200，响应 <0.5s）。
- 验证：admin/admin123 登录成功，返回 token 与 admin 角色；说明 MongoDB 连接正常、初始管理员已 seed。
- 后续：用户需在浏览器中打开网址，先改 admin 默认密码，再给同事创建账号。
- 持久化：数据现在存于 MongoDB Atlas，Railway 重启/重部署不会丢失。
